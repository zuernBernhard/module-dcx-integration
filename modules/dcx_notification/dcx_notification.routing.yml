dcx_notification.trigger:
  path: '/dcx-notification'
  defaults:
    _controller: '\Drupal\dcx_notification\Responder::trigger'
  requirements:
    _permission: 'administer nodes'
  options:
    _auth: ['basic_auth', 'cookie']