dcx_integration.json_client_settings: #name it same as route to avoid confusion
  title: DCX Credentials
  #route defined in .routing.yml
  route_name: dcx_integration.json_client_settings #link to tab1 route
  #route defined in .routing.yml
  base_route: dcx_integration.json_client_settings #set the base tab
  weight: 0

